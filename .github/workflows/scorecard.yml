# OpenSSF Scorecard — free OSS alternative to GHAS, no paid tier required
# Publishes results to code scanning + provides badge via api.scorecard.dev
name: Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 7 * * 1' # weekly
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  id-token: write # for sigstore keyless signing
  actions: read

jobs:
  analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: ossf/scorecard-action@v2.4.1
        with:
          results_file: results.sarif
          results_format: sarif
          # ponytail: publish_results needs id-token:write — free for public repos
          publish_results: true

      - uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
      - uses: sigstore/cosign-installer@v3.9.2
      - run: cosign version || true
